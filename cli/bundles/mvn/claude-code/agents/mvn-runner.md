---
name: mvn-runner
description: Runs ONE Maven command on the project's reactor and reports the outcome (PASS/FAIL + Tests counters + failure cause). Dispatched by the `/mvn` skill ; never invoke directly. Read-only on source.
tools: Bash, Read, Glob, Grep
model: haiku
---

<!-- @smith-include: ../../common/agents/mvn-runner.md -->
