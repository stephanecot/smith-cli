{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://smith-cli/providers/specs/provider.schema.json",
  "title": "Smith provider — provider.yaml",
  "description": "Top-level YAML for a Smith AI-provider folder under cli/providers/<slug>/. Declares the provider's identity, the per-kind format files, discovery precedence, cross-kind interactions, and the decision matrix authors use to pick a kind.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "name",
    "description",
    "docs",
    "kinds"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Provider slug. MUST equal the parent directory name."
    },
    "name": {
      "type": "string",
      "description": "Human-readable provider name."
    },
    "description": {
      "type": "string",
      "description": "One-paragraph capability summary."
    },
    "docs": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "string",
        "format": "uri"
      },
      "description": "Map of named links to the provider's official documentation pages."
    },
    "kinds": {
      "type": "object",
      "additionalProperties": false,
      "required": ["agent", "skill", "hook"],
      "description": "Per-kind index — MUST list agent, skill, hook (each with a matching format-*.yaml). May also list rules (no format file — minimal entry with consumer_path + invocation).",
      "properties": {
        "agent": { "$ref": "#/$defs/kindEntry" },
        "skill": { "$ref": "#/$defs/kindEntry" },
        "hook":  { "$ref": "#/$defs/kindEntry" },
        "rules": { "$ref": "#/$defs/kindEntryRules" }
      }
    },
    "discovery": {
      "type": "object",
      "additionalProperties": false,
      "required": ["precedence", "identity_rules"],
      "properties": {
        "precedence": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["scope", "path", "priority"],
            "properties": {
              "scope":    { "type": "string" },
              "path":     { "type": "string" },
              "priority": { "type": "integer", "minimum": 1 }
            }
          }
        },
        "identity_rules": {
          "type": "string",
          "description": "How identical slugs are resolved across scopes."
        }
      }
    },
    "cross_kind_interactions": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" }
    },
    "naming_conventions": {
      "type": "array",
      "items": { "type": "string" }
    },
    "when_to_pick": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["need", "use"],
        "properties": {
          "need": { "type": "string" },
          "use":  { "type": "string" }
        }
      }
    },
    "smith_mapping": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "$defs": {
    "kindEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["format", "consumer_path", "invocation", "example"],
      "properties": {
        "format": {
          "type": "string",
          "pattern": "^format-(agent|skill|hook)\\.yaml$",
          "description": "Relative path to the matching format-*.yaml file."
        },
        "consumer_path": {
          "type": "string",
          "description": "Where this kind lives in a consumer project."
        },
        "invocation": {
          "type": "string",
          "description": "How the user (or the AI) invokes this kind."
        },
        "example": {
          "type": "string",
          "pattern": "^example/example-(agent|skill|hook)[^/]*\\.[^/]+$",
          "description": "Relative path to the worked-out example under example/. Filename MUST start with example-<kind> and may carry a provider-specific suffix before the extension (e.g. example-agent.agent.md, example-hook-tasks.json)."
        }
      }
    },
    "kindEntryRules": {
      "type": "object",
      "additionalProperties": false,
      "required": ["consumer_path", "invocation"],
      "description": "Optional `rules` kind. Rules surfaces (CLAUDE.md, AGENTS.md, .github/copilot-instructions.md, ...) carry minimal or no frontmatter and do NOT get a dedicated format-*.yaml or worked-out example.",
      "properties": {
        "consumer_path": {
          "type": "string",
          "description": "Where the rules artefact lives in a consumer project."
        },
        "invocation": {
          "type": "string",
          "description": "How rules are loaded (typically always-on or path-scoped)."
        }
      }
    }
  }
}
