# Changelog — Java + Spring Boot 4 template set

All notable changes to this framework template set are recorded here.

## [0.1.0] — initial

- 1 skill authored : bootstrap (`pom.xml`, application entry, optional
  REST controller, optional Liquibase changelog, `application.yaml`).
- `mvn -B verify` smoke check at the end of the scaffold.
- Adapter placeholders : `{{language}}`, `{{runtime}}`, `{{framework}}`,
  `{{framework_version}}`, `{{root_package}}`, `{{project_name}}`.
