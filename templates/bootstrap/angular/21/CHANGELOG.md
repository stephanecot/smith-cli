# Changelog — Angular 21 bootstrap

All notable changes to this bootstrap template are recorded here.

## [0.1.0] — initial

- Moved from `framework/angular/21/skills/bootstrap` into the
  `bootstrap/` category. Same body — interactive scaffold of an
  Angular 21 SPA with `npm run build` smoke check.
- 0 assets, 0 templates, 0 scripts (the body uses provider-native
  tooling — `Write` / `Bash` — to create the project tree).
